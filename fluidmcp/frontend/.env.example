# ⚠️  DEVELOPMENT MODE ONLY - DO NOT USE FOR PRODUCTION BUILDS!
#
# This file is ONLY for running "npm run dev" (separate frontend server).
# For production builds ("npm run build"), DO NOT set VITE_API_BASE_URL.
#
# When VITE_API_BASE_URL is unset, the built frontend uses relative paths,
# allowing it to work on ANY port where the backend is running.

# ============================================================================
# Development Mode Configuration
# ============================================================================

# Local development (backend on default port)
VITE_API_BASE_URL=http://localhost:8099

# GitHub Codespaces (replace with your codespace name)
# VITE_API_BASE_URL=https://<your-codespace-name>-8099.app.github.dev

# Custom port (if backend runs on different port)
# VITE_API_BASE_URL=http://localhost:8100

# ============================================================================
# Production Build
# ============================================================================
#
# For production deployment:
# 1. Remove or rename this .env file
# 2. Run: npm run build
# 3. The built frontend will use relative paths (works on any port)
# 4. Deploy: fmcp serve (serves frontend at /ui on same port as backend)
